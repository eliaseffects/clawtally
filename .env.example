# HMAC secret for anonymous token signing
CLAWBOARD_TOKEN_SECRET=change-this-in-production

# Optional: require this header value for POST /api/pair/register
# x-clawboard-pair-secret: <value>
CLAWBOARD_PAIRING_SECRET=

# Optional: enables deterministic mock stats when gateway calls fail
# Leave false in production.
CLAWBOARD_ALLOW_MOCK_STATS=false

# Optional: lower bound for "all-time" usage requests
# CLAWBOARD_ALL_TIME_START_DATE=1970-01-01

# Local connector daemon settings (npm run connector)
CLAWBOARD_CONNECTOR_HOST=127.0.0.1
CLAWBOARD_CONNECTOR_PORT=18888
# Comma-separated list. Supports '*' and prefix wildcards like https://*.example.com*
CLAWBOARD_CONNECTOR_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://clawboard.io,https://www.clawboard.io
# Optional explicit config file path for connector
# CLAWBOARD_CONNECTOR_GATEWAY_CONFIG=/absolute/path/to/config.json
# Optional dashboard URL used by connector /v1/clawboard/pair
# CLAWBOARD_DASHBOARD_URL=https://clawboard.io
# Gateway values connector can serve directly
# CLAWBOARD_GATEWAY_URL=ws://localhost:18789
# CLAWBOARD_GATEWAY_TOKEN=
